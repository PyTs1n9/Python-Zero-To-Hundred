## 039 合并有序列表

给定两个按非递减顺序排列的整数列表 `a` 和 `b`，将它们合并成一个新的有序列表。

本题要求使用双指针完成，不要把两个列表拼接后再整体排序。

#### 示例 1：

> 输入：a = [1, 2, 4], b = [1, 3, 4]
>
> 输出：[1, 1, 2, 3, 4, 4]

#### 示例 2：

> 输入：a = [], b = [0]
>
> 输出：[0]

#### 提示：

- `0 <= len(a), len(b) <= 10^5`
- 不要遗漏任一列表末尾的剩余元素

