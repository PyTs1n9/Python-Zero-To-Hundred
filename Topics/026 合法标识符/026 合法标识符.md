## 026 合法标识符

给定字符串 `name`，判断它能否作为一个普通的 Python 变量名。

合法变量名必须是合法标识符，并且不能是 Python 关键字。

#### 示例 1：

> 输入：name = "student_2"
>
> 输出：true

#### 示例 2：

> 输入：name = "for"
>
> 输出：false

#### 提示：

- `1 <= len(name) <= 100`
- 可以使用字符串方法和 `keyword` 标准库

