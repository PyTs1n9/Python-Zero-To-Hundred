## 022 忽略符号的回文串

给定字符串 `s`，只保留其中的字母和数字，并忽略字母大小写，判断处理后的字符串是否为回文串。

#### 示例 1：

> 输入：s = "A man, a plan, a canal: Panama"
>
> 输出：true

#### 示例 2：

> 输入：s = "race a car"
>
> 输出：false

#### 提示：

- `0 <= len(s) <= 10^5`
- 可使用字符串方法判断字符类型并统一大小写
- 处理后为空串时返回 `True`

