## 028 凯撒密码

给定字符串 `text` 和整数位移量 `shift`，对其中的英文字母进行凯撒加密。

大写字母和小写字母分别循环位移，非英文字母保持不变。

#### 示例 1：

> 输入：text = "Abc-Z", shift = 2
>
> 输出："Cde-B"

#### 示例 2：

> 输入：text = "Python 3!", shift = -1
>
> 输出："Oxsgnm 3!"

#### 提示：

- `0 <= len(text) <= 10^5`
- `-10^9 <= shift <= 10^9`
- 先把位移量化到 `0~25`

