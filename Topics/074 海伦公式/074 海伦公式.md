## 074 海伦公式

给定三角形三边 `a`、`b`、`c`，先判断它们能否组成三角形，再用海伦公式计算面积。

若不能组成三角形，返回 `None`；否则返回四舍五入到两位小数的面积。

#### 示例 1：

> 输入：a = 3, b = 4, c = 5
>
> 输出：6.0

#### 示例 2：

> 输入：a = 1, b = 2, c = 3
>
> 输出：null

#### 提示：

- `0 < a, b, c <= 10^6`
- 半周长 `p = (a + b + c) / 2`
- 面积 `sqrt(p(p-a)(p-b)(p-c))`

