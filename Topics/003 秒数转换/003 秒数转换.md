## 003 秒数转换

给定一个非负整数 `seconds`，将它转换成“时、分、秒”。

返回三元组 `(hour, minute, second)`，其中 `minute` 和 `second` 的取值范围均为 `0` 到 `59`。

#### 示例 1：

> 输入：seconds = 3661
>
> 输出：(1, 1, 1)

#### 示例 2：

> 输入：seconds = 59
>
> 输出：(0, 0, 59)

#### 提示：

- `0 <= seconds <= 10^9`
- 尝试使用整除 `//` 和取余 `%`

