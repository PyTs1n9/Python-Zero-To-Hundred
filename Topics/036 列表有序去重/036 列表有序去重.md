## 036 列表有序去重

给定列表 `items`，删除其中重复元素，同时保持每个元素第一次出现的顺序。

列表元素均为可哈希对象。

#### 示例 1：

> 输入：items = [3, 1, 3, 2, 1]
>
> 输出：[3, 1, 2]

#### 示例 2：

> 输入：items = ["a", "a", "b", "A"]
>
> 输出：["a", "b", "A"]

#### 提示：

- `0 <= len(items) <= 10^5`
- 集合适合快速判断元素是否出现过

