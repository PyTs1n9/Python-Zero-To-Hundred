## 033 括号匹配

给定只包含 `()`、`[]`、`{}` 六种字符的字符串 `s`，判断其中的括号是否正确匹配。

正确匹配要求每个左括号都有相同类型的右括号，并且闭合顺序正确。

#### 示例 1：

> 输入：s = "{[()]}"
>
> 输出：true

#### 示例 2：

> 输入：s = "([)]"
>
> 输出：false

#### 提示：

- `0 <= len(s) <= 10^5`
- 可以用列表模拟栈

