## 038 列表循环右移

给定列表 `nums` 和非负整数 `k`，将列表循环向右移动 `k` 位并返回新列表。

#### 示例 1：

> 输入：nums = [1, 2, 3, 4, 5], k = 2
>
> 输出：[4, 5, 1, 2, 3]

#### 示例 2：

> 输入：nums = [1, 2], k = 5
>
> 输出：[2, 1]

#### 提示：

- `0 <= len(nums) <= 10^5`
- `0 <= k <= 10^9`
- 空列表应直接返回空列表

