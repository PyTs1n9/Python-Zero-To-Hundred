## 034 最长公共前缀

给定一个字符串列表 `words`，返回这些字符串的最长公共前缀。如果不存在公共前缀，返回空字符串。

#### 示例 1：

> 输入：words = ["flower", "flow", "flight"]
>
> 输出："fl"

#### 示例 2：

> 输入：words = ["dog", "racecar", "car"]
>
> 输出：""

#### 提示：

- `1 <= len(words) <= 1000`
- `0 <= len(words[i]) <= 1000`

