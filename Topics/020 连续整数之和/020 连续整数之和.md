## 020 连续整数之和

给定正整数 `n`，找出所有由两个或更多连续正整数相加得到 `n` 的方案。

每个方案用列表表示，所有方案按首项从小到大排列。

#### 示例 1：

> 输入：n = 15
>
> 输出：[[1, 2, 3, 4, 5], [4, 5, 6], [7, 8]]

#### 示例 2：

> 输入：n = 8
>
> 输出：[]

#### 提示：

- `1 <= n <= 10000`
- 当连续数字之和超过 `n` 时，应及时停止当前尝试

