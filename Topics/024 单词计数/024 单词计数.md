## 024 单词计数

给定英文文本 `text`，单词之间可能有一个或多个空白字符。返回单词总数。

#### 示例 1：

> 输入：text = "Python is easy"
>
> 输出：3

#### 示例 2：

> 输入：text = "  one\ttwo\nthree  "
>
> 输出：3

#### 提示：

- `0 <= len(text) <= 10^5`
- 不带参数的 `split()` 可以处理连续空白

