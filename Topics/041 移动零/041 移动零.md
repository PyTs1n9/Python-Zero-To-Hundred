## 041 移动零

给定整数列表 `nums`，将所有 `0` 移到列表末尾，同时保持非零元素的相对顺序。

返回处理后的列表。

#### 示例 1：

> 输入：nums = [0, 1, 0, 3, 12]
>
> 输出：[1, 3, 12, 0, 0]

#### 示例 2：

> 输入：nums = [0, 0, 1]
>
> 输出：[1, 0, 0]

#### 提示：

- `0 <= len(nums) <= 10^5`
- 不要对非零元素重新排序

