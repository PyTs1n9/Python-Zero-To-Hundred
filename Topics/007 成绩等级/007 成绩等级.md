## 007 成绩等级

给定整数成绩 `score`，按规则返回等级：`90~100` 为 `A`，`80~89` 为 `B`，`70~79` 为 `C`，`60~69` 为 `D`，低于 `60` 为 `E`。

如果成绩不在 `0~100` 内，返回 `"ERROR"`。

#### 示例 1：

> 输入：score = 86
>
> 输出："B"

#### 示例 2：

> 输入：score = 105
>
> 输出："ERROR"

#### 提示：

- `score` 为整数
- 可以使用多分支结构完成

