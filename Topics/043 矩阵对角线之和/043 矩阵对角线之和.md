## 043 矩阵对角线之和

给定一个 `n × n` 方阵，计算主对角线与副对角线上所有元素之和。

如果 `n` 为奇数，中心元素只能计算一次。

#### 示例 1：

> 输入：matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
>
> 输出：25

#### 示例 2：

> 输入：matrix = [[1, 2], [3, 4]]
>
> 输出：10

#### 提示：

- `1 <= n <= 1000`
- 主对角线满足 `行下标 == 列下标`

