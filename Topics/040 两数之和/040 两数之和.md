## 040 两数之和

给定整数列表 `nums` 和目标值 `target`，找出和为目标值的两个元素下标并返回 `[i, j]`，其中 `i < j`。

每组输入保证恰好有一个答案，同一元素不能使用两次。

#### 示例 1：

> 输入：nums = [2, 7, 11, 15], target = 9
>
> 输出：[0, 1]

#### 示例 2：

> 输入：nums = [3, 2, 4], target = 6
>
> 输出：[1, 2]

#### 提示：

- `2 <= len(nums) <= 10^5`
- 尝试用字典记录已经遍历过的数字及其下标

