## 032 十进制转任意进制

给定非负十进制整数 `n` 和进制 `base`，将 `n` 转换成对应进制的字符串，字母位使用大写 `A~F`。

#### 示例 1：

> 输入：n = 45, base = 2
>
> 输出："101101"

#### 示例 2：

> 输入：n = 255, base = 16
>
> 输出："FF"

#### 提示：

- `0 <= n <= 10^18`
- `2 <= base <= 16`
- `n = 0` 时返回 `"0"`
- 使用“除基取余”法

