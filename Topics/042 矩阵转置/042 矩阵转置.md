## 042 矩阵转置

给定一个 `m × n` 的二维列表 `matrix`，返回它的转置矩阵。

转置后，原矩阵第 `i` 行第 `j` 列的元素位于第 `j` 行第 `i` 列。

#### 示例 1：

> 输入：matrix = [[1, 2, 3], [4, 5, 6]]
>
> 输出：[[1, 4], [2, 5], [3, 6]]

#### 示例 2：

> 输入：matrix = [[1], [2], [3]]
>
> 输出：[[1, 2, 3]]

#### 提示：

- `1 <= m, n <= 100`
- 每一行的长度相同

