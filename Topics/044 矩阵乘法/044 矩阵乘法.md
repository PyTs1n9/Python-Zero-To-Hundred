## 044 矩阵乘法

给定 `m × n` 矩阵 `a` 和 `n × p` 矩阵 `b`，计算并返回它们的乘积矩阵。

#### 示例 1：

> 输入：a = [[1, 2], [3, 4]], b = [[5, 6], [7, 8]]
>
> 输出：[[19, 22], [43, 50]]

#### 示例 2：

> 输入：a = [[1, 2, 3]], b = [[1], [2], [3]]
>
> 输出：[[14]]

#### 提示：

- `1 <= m, n, p <= 50`
- 结果的形状为 `m × p`
- 用三层循环理解“行乘列”的过程

