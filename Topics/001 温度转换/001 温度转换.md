## 001 温度转换

给定一个摄氏温度 `c`，将它转换为华氏温度。转换公式为 `f = c × 1.8 + 32`。

结果保留两位小数，并以字符串形式返回。

#### 示例 1：

> 输入：c = 0
>
> 输出："32.00"

#### 示例 2：

> 输入：c = 36.5
>
> 输出："97.70"

#### 提示：

- `-273.15 <= c <= 10000`
- 使用格式化方式保留两位小数

