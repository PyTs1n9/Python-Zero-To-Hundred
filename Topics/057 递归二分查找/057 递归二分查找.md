## 057 递归二分查找

给定一个升序且元素互不相同的整数列表 `nums` 和目标值 `target`，使用递归二分查找返回目标值下标。

如果目标不存在，返回 `-1`。

#### 示例 1：

> 输入：nums = [-1, 0, 3, 5, 9, 12], target = 9
>
> 输出：4

#### 示例 2：

> 输入：nums = [-1, 0, 3, 5, 9, 12], target = 2
>
> 输出：-1

#### 提示：

- `0 <= len(nums) <= 10^5`
- 每次递归都应缩小搜索区间

