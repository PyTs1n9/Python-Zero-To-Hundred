## 058 汉诺塔步骤

有 `n` 个大小不同的圆盘，从小到大叠放在柱子 `A` 上。每次只能移动一个圆盘，且大盘不能放在小盘上。

使用递归返回把全部圆盘从 `A` 移到 `C` 的操作列表，每项格式为 `"A->C"`，可借助柱子 `B`。

#### 示例 1：

> 输入：n = 1
>
> 输出：["A->C"]

#### 示例 2：

> 输入：n = 2
>
> 输出：["A->B", "A->C", "B->C"]

#### 提示：

- `1 <= n <= 15`
- 总移动次数为 `2^n - 1`

