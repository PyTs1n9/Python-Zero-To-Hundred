## 012 九九乘法表

生成九九乘法表并以字符串列表返回。第 `i` 行包含从 `1×i=i` 到 `i×i=i²` 的算式，相邻算式之间用一个空格分隔。

#### 示例 1：

> 输入：无
>
> 输出的前 3 项：["1×1=1", "1×2=2 2×2=4", "1×3=3 2×3=6 3×3=9"]

#### 示例 2：

> 输入：无
>
> 输出列表长度：9

#### 提示：

- 使用两层循环
- 返回列表中的每一项代表一行，不要在行尾添加空格

