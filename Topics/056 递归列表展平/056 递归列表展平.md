## 056 递归列表展平

给定一个由整数和嵌套列表组成的列表 `items`，使用递归将其中所有整数按原顺序放入一维列表。

#### 示例 1：

> 输入：items = [1, [2, [3, 4]], 5]
>
> 输出：[1, 2, 3, 4, 5]

#### 示例 2：

> 输入：items = [[], [1, []], 2]
>
> 输出：[1, 2]

#### 提示：

- 嵌套深度不超过 `100`
- 使用 `isinstance(item, list)` 判断当前元素是否为列表

