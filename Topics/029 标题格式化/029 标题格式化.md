## 029 标题格式化

给定由若干英文单词组成的字符串 `title`。单词之间可能有多个空白字符。

将每个单词改成首字母大写、其余字母小写，并用单个空格连接后返回。

#### 示例 1：

> 输入：title = "python ZERO to WIN"
>
> 输出："Python Zero To Win"

#### 示例 2：

> 输入：title = "  hELLO   world  "
>
> 输出："Hello World"

#### 提示：

- `0 <= len(title) <= 10^5`
- 输入为空白字符串时返回空字符串

