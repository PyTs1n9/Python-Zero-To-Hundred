## 030 最长单词

给定只包含英文字母和空格的字符串 `text`，返回其中最长的单词。

如果有多个单词长度相同，返回最先出现的那个；如果没有单词，返回空字符串。

#### 示例 1：

> 输入：text = "Python makes programming enjoyable"
>
> 输出："programming"

#### 示例 2：

> 输入：text = "cat dog fox"
>
> 输出："cat"

#### 提示：

- `0 <= len(text) <= 10^5`
- 比较时不要破坏原有单词内容

