## 014 区间素数

给定两个整数 `left` 和 `right`，返回闭区间 `[left, right]` 内的全部素数，结果按升序排列。

#### 示例 1：

> 输入：left = 10, right = 20
>
> 输出：[11, 13, 17, 19]

#### 示例 2：

> 输入：left = 1, right = 2
>
> 输出：[2]

#### 提示：

- `-1000 <= left <= right <= 10000`
- 可以复用“素数判断”函数

