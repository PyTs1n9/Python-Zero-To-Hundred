## 006 闰年判断

给定一个年份 `year`，判断它是否为闰年。

能被 `400` 整除，或者能被 `4` 整除但不能被 `100` 整除的年份是闰年。

#### 示例 1：

> 输入：year = 2000
>
> 输出：true

#### 示例 2：

> 输入：year = 1900
>
> 输出：false

#### 提示：

- `1 <= year <= 9999`
- 返回布尔值 `True` 或 `False`

