## 090 中文词频统计

给定中文文本 `text`，使用 `jieba.lcut()` 分词，统计长度大于 `1` 的词语出现次数。

忽略纯空白和纯标点结果，返回按次数降序、词语升序排列的 `(词语, 次数)` 列表。

#### 示例 1：

> 输入：text = "学习Python，快乐学习Python。"
>
> 输出中："Python" 和 "学习" 的次数均为 2

#### 示例 2：

> 输入：text = "，。！？"
>
> 输出：[]

#### 提示：

- 可以用 `word.strip()` 和字符判断方法过滤无效词
- 使用字典统计词频

