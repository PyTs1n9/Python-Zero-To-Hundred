## 017 最小公倍数

给定两个整数 `a` 和 `b`，返回它们的最小公倍数。

只要其中一个数为 `0`，结果就为 `0`；其他情况下结果为正数。

#### 示例 1：

> 输入：a = 12, b = 18
>
> 输出：36

#### 示例 2：

> 输入：a = -4, b = 6
>
> 输出：12

#### 提示：

- `-10^9 <= a, b <= 10^9`
- 非零时可利用最大公约数减少计算量

