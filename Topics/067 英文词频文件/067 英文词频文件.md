## 067 英文词频文件

读取 UTF-8 英文文本文件，统计单词出现次数。

单词由连续英文字母组成，忽略大小写和其他符号。返回按次数降序、单词升序排列的 `(单词, 次数)` 列表。

#### 示例 1：

> 文件内容：`Python is good. PYTHON is fun!`
>
> 输出：[("is", 2), ("python", 2), ("fun", 1), ("good", 1)]

#### 示例 2：

> 文件内容：`123, !!!`
>
> 输出：[]

#### 提示：

- 可以逐字符把非英文字母替换成空格
- 排序时使用两个关键字

