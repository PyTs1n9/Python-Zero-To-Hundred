## 035 字符串压缩

给定只包含英文字母的字符串 `s`，将连续相同字符压缩成“字符 + 次数”。次数为 `1` 时也要保留。

#### 示例 1：

> 输入：s = "aaabbc"
>
> 输出："a3b2c1"

#### 示例 2：

> 输入：s = "abcd"
>
> 输出："a1b1c1d1"

#### 提示：

- `0 <= len(s) <= 10^5`
- 空字符串返回空字符串
- 只压缩相邻且相同的字符

