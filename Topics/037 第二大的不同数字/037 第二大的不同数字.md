## 037 第二大的不同数字

给定整数列表 `nums`，返回其中第二大的不同数字。如果不存在，返回 `None`。

#### 示例 1：

> 输入：nums = [2, 3, 1, 3]
>
> 输出：2

#### 示例 2：

> 输入：nums = [5, 5]
>
> 输出：null

#### 提示：

- `0 <= len(nums) <= 10^5`
- `-10^9 <= nums[i] <= 10^9`
- 相同数值只算一种数字

