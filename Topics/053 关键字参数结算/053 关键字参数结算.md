## 053 关键字参数结算

实现函数 `checkout(discount=1.0, **products)`。每个关键字参数的名称是商品名，值是该商品的小计。

返回折扣后的总金额，结果四舍五入到两位小数。

#### 示例 1：

> 输入：checkout(discount=0.8, book=50, pen=10)
>
> 输出：48.0

#### 示例 2：

> 输入：checkout()
>
> 输出：0.0

#### 提示：

- `0 <= discount <= 1`
- 商品小计均为非负数
- 练习默认参数和 `**kwargs`

