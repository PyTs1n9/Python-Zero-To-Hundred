## 016 最大公约数

给定两个整数 `a` 和 `b`，使用辗转相除法求它们的最大公约数。

结果始终返回非负整数；规定 `gcd(0, 0) = 0`。

#### 示例 1：

> 输入：a = 48, b = 18
>
> 输出：6

#### 示例 2：

> 输入：a = -12, b = 8
>
> 输出：4

#### 提示：

- `-10^9 <= a, b <= 10^9`
- 核心关系为 `gcd(a, b) = gcd(b, a % b)`

